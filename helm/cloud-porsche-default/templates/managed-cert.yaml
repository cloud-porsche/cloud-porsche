apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  name: managed-cert
  namespace: {{ .Release.Namespace}}
spec:
  domains:
    - property-management.{{ .Values.tenantId }}.cloud-porsche.com
    - parking-management.{{ .Values.tenantId }}.cloud-porsche.com
    - monitoring-management.{{ .Values.tenantId }}.cloud-porsche.com
